/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import albumsSaga from '../saga';

// const generator = albumsSaga();

describe('albumsSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
