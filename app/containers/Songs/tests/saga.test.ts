/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import songsSaga from '../saga';

// const generator = songsSaga();

describe('songsSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
